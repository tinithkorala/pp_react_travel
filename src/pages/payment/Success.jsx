import { useParams } from "react-router-dom";
import PaymentContent from "../../components/page_components/payment/PaymentContent";

const Success = () => {

  return <PaymentContent stage={true} />;
};

export default Success;
